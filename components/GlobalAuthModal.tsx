'use client';

/**
 * Placeholder global auth modal host.
 * Page-level AuthModal is used for prompts; this keeps layout imports valid.
 */
export default function GlobalAuthModal() {
  return null;
}